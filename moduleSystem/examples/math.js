console.log("math.js is being executed...");

const PI = 3.14159;

function add(a, b) {
    return a + b;
}

function multiply(a, b) {
    return a * b;
}

module.exports = {
    PI,
    add,
    multiply
};
